{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "create installer",
            "type": "shell",
            "options": {
                "cwd": "${workspaceRoot}/scripts"
            },
            "command": "iscc.exe ufprog.iss",
            "group": "build"
        },
    ]
}